<UserControl x:Class="IMDB.Views.MovieListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IMDB.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1120">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and App Logo -->
        <Grid Grid.Row="0">
            <Button Content="Back to genres" HorizontalAlignment="Left" Command="{Binding BackToGenresCommand}" Padding="10,5"/>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <TextBlock Text="IMDB Search App" VerticalAlignment="Center" Foreground="White" Margin="0,0,10,0"/>
                <Border Width="40" Height="40" Background="#FF5722" CornerRadius="20">
                    <TextBlock Text="🎬" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Genre Title -->
        <TextBlock Grid.Row="1" Text="{Binding GenreName, StringFormat=Genre: {0}}" FontWeight="Bold" FontSize="24" Margin="0,20,0,20" Foreground="White"/>

        <!-- Movies List -->
        <ListView Grid.Row="2" ItemsSource="{Binding Titles}" Background="Transparent" BorderThickness="0">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="#3a3352"/>
                    <Setter Property="Margin" Value="0,5"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding PrimaryTitle}" FontWeight="Bold" FontSize="16" Foreground="White"/>
                        <TextBlock Text="{Binding TitleType}" Foreground="#cccccc" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding Rating.AverageRating, StringFormat=Rating: {0:N1}/10}" Foreground="#ffcc00" Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>